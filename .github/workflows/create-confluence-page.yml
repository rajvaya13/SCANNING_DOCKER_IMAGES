name: Create Confluence Page

on:
  push:
    branches:
      - confluence
    paths:
      - 'change_folder/**'


jobs:
  merge_workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Print
        run: echo "Hello"
