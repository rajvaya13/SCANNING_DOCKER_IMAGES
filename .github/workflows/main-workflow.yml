name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  main_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      # Call the sub-workflow action
      - name: Run Sub Workflow
        uses: ./.github/actions/my-sub-workflow-action
