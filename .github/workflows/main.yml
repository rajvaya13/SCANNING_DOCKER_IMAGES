name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        #abc
      - name: Listing dkr
        run: |
          pwd
          ls
          ls -la

      - name: Run Workflow A Steps
        uses: ./home/runner/work/Test/Test/.github/actions
