name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        #abc
      - name: Listing dkr
        run: |
          pwd
          ls

      - name: Run Workflow A Steps
        uses: ./.github/actions
